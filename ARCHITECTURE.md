# 🏗️ Application Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────┐
│                     PhotoEditor                          │
│         CSS-Based Photo Editor with Cloud Storage        │
└─────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
    ┌───▼───┐          ┌────▼────┐        ┌────▼────┐
    │Frontend│          │ Backend │        │Database │
    │Next.js │◄────────►│Next.js  │◄──────►│MongoDB  │
    │React   │          │API      │        │Mongoose │
    └────────┘          └─────────┘        └─────────┘
```

## Frontend Architecture

```
┌─────────────────────────────────────────────┐
│              User Interface                  │
├─────────────────────────────────────────────┤
│                                             │
│  ┌────────────────────────────────────┐    │
│  │      Authentication Pages          │    │
│  │  • Sign In  • Sign Up              │    │
│  └────────────────────────────────────┘    │
│                                             │
│  ┌────────────────────────────────────┐    │
│  │      Main Dashboard                │    │
│  │  • Search Bar                      │    │
│  │  • Upload Button                   │    │
│  │  • Image Gallery Grid              │    │
│  │  • User Info & Stats               │    │
│  └────────────────────────────────────┘    │
│                                             │
│  ┌────────────────────────────────────┐    │
│  │      Photo Editor (Modal)          │    │
│  │  • Filter Sidebar (9 filters)     │    │
│  │  • Canvas Preview Area             │    │
│  │  • Save/Close Toolbar              │    │
│  └────────────────────────────────────┘    │
│                                             │
│  ┌────────────────────────────────────┐    │
│  │      Image Upload (Modal)          │    │
│  │  • File Selector                   │    │
│  │  • Compression Options             │    │
│  │  • Metadata Inputs (Title/Tags)    │    │
│  └────────────────────────────────────┘    │
│                                             │
└─────────────────────────────────────────────┘
```

## Component Hierarchy

```
App (Root)
│
├── Layout (app/layout.tsx)
│   ├── Global Styles
│   ├── Fonts
│   └── Metadata
│
└── Page (app/page.tsx)
    │
    └── SessionProvider
        │
        └── Dashboard
            ├── Navigation Bar
            │   ├── Logo & Title
            │   ├── User Info
            │   ├── Usage Counter (X/10)
            │   └── Sign Out Button
            │
            ├── Search & Actions Bar
            │   ├── Search Input
            │   ├── Search Button
            │   └── Upload Button
            │
            ├── Image Gallery Grid
            │   └── Image Card (×N)
            │       ├── Image Preview
            │       ├── Hover Actions
            │       │   ├── Edit Button
            │       │   └── Delete Button
            │       └── Metadata
            │           ├── Title (editable)
            │           ├── Tags (editable)
            │           └── Info (date, size)
            │
            ├── Image Upload Modal (conditional)
            │   ├── File Input
            │   ├── Preview
            │   ├── Compression Options
            │   ├── Title Input
            │   ├── Tags Input
            │   └── Upload Button
            │
            └── Photo Editor Modal (conditional)
                ├── Filter Sidebar
                │   ├── Filter Control (×9)
                │   │   ├── Label
                │   │   ├── Slider
                │   │   └── Value Display
                │   └── Reset Button
                │
                ├── Toolbar
                │   ├── Save Button
                │   └── Close Button
                │
                └── Canvas Area
                    └── Image with Filters
```

## Data Flow

### 1. User Authentication Flow

```
Sign Up Page
    │
    ├─► Enter Details (name, email, password)
    │
    └─► POST /api/auth/register
            │
            ├─► Hash Password (bcryptjs)
            │
            ├─► Save to MongoDB (User collection)
            │
            └─► Redirect to Sign In
                    │
                    └─► Sign In Page
                            │
                            ├─► Enter Credentials
                            │
                            └─► POST /api/auth/signin (NextAuth)
                                    │
                                    ├─► Verify Password
                                    │
                                    ├─► Create JWT Session
                                    │
                                    └─► Redirect to Dashboard
```

### 2. Image Upload Flow

```
Dashboard
    │
    ├─► Click Upload Button
    │
    └─► Image Upload Modal
            │
            ├─► Select File
            │
            ├─► Check Size
            │   │
            │   ├─► < 5MB ──► Continue
            │   │
            │   └─► > 5MB ──► Compression Options
            │                     │
            │                     ├─► High Quality (80%)
            │                     ├─► Medium Quality (60%)
            │                     └─► Low Quality (40%)
            │                             │
            │                             └─► Compress (browser-image-compression)
            │
            ├─► Enter Title & Tags
            │
            └─► POST /api/images
                    │
                    ├─► Validate Session
                    │
                    ├─► Check Image Limit (10 max)
                    │
                    ├─► Convert to Base64
                    │
                    ├─► Save to MongoDB (Image collection)
                    │
                    └─► Return Success
                            │
                            └─► Refresh Gallery
```

### 3. Image Editing Flow

```
Image Gallery
    │
    ├─► Click Edit Button on Image
    │
    └─► Photo Editor Modal
            │
            ├─► Load Image Data
            │
            ├─► Display in Canvas
            │
            └─► User Adjusts Filters
                    │
                    ├─► Real-time CSS Filter Application
                    │   (No server calls)
                    │
                    └─► Click Save
                            │
                            ├─► Apply Filters to Canvas
                            │
                            ├─► Export as Base64
                            │
                            └─► PUT /api/images/[id]
                                    │
                                    ├─► Validate Session
                                    │
                                    ├─► Update MongoDB
                                    │
                                    └─► Return Success
                                            │
                                            └─► Refresh Gallery
```

### 4. Search Flow

```
Dashboard Search Bar
    │
    ├─► User Types Query
    │
    └─► Submit Search
            │
            └─► GET /api/images?search=query
                    │
                    ├─► Validate Session
                    │
                    ├─► MongoDB Query
                    │   │
                    │   ├─► Search in Title (regex)
                    │   └─► Search in Tags (array match)
                    │
                    └─► Return Matching Images
                            │
                            └─► Update Gallery Display
```

## API Architecture

```
┌────────────────────────────────────────┐
│           API Routes                   │
├────────────────────────────────────────┤
│                                        │
│  /api/auth/                            │
│  ├── [...nextauth]/route.ts           │
│  │   ├── GET  (NextAuth handler)      │
│  │   └── POST (NextAuth handler)      │
│  │                                     │
│  └── register/route.ts                 │
│      └── POST (Create user)            │
│                                        │
│  /api/images/                          │
│  ├── route.ts                          │
│  │   ├── GET  (List all user images)  │
│  │   └── POST (Upload new image)      │
│  │                                     │
│  └── [id]/route.ts                     │
│      ├── GET    (Get specific image)  │
│      ├── PUT    (Update image)        │
│      └── DELETE (Delete image)        │
│                                        │
└────────────────────────────────────────┘
```

## Database Schema

```
┌─────────────────────────────────────────┐
│            MongoDB Database             │
├─────────────────────────────────────────┤
│                                         │
│  Users Collection                       │
│  ┌───────────────────────────────────┐ │
│  │ _id: ObjectId                     │ │
│  │ email: String (unique, indexed)   │ │
│  │ password: String (hashed)         │ │
│  │ name: String                      │ │
│  │ createdAt: Date                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  Images Collection                      │
│  ┌───────────────────────────────────┐ │
│  │ _id: ObjectId                     │ │
│  │ userId: String (indexed) ─────────┼─┤
│  │ title: String (indexed)           │ │
│  │ tags: Array<String> (indexed)     │ │
│  │ imageData: String (Base64)        │ │
│  │ originalSize: Number              │ │
│  │ createdAt: Date                   │ │
│  │ updatedAt: Date                   │ │
│  └───────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

## Security Layers

```
┌──────────────────────────────────────────┐
│         Security Architecture            │
├──────────────────────────────────────────┤
│                                          │
│  Layer 1: Authentication                 │
│  ┌────────────────────────────────────┐ │
│  │ NextAuth.js JWT Sessions           │ │
│  │ Password Hashing (bcryptjs)        │ │
│  │ Secure Cookies                     │ │
│  └────────────────────────────────────┘ │
│                                          │
│  Layer 2: Authorization                  │
│  ┌────────────────────────────────────┐ │
│  │ Session Validation (all routes)    │ │
│  │ User-specific Data Access          │ │
│  │ Protected API Endpoints            │ │
│  └────────────────────────────────────┘ │
│                                          │
│  Layer 3: Input Validation               │
│  ┌────────────────────────────────────┐ │
│  │ Type Checking (TypeScript)         │ │
│  │ Schema Validation (Mongoose)       │ │
│  │ File Size Validation               │ │
│  │ Format Validation                  │ │
│  └────────────────────────────────────┘ │
│                                          │
│  Layer 4: Environment                    │
│  ┌────────────────────────────────────┐ │
│  │ Secret Keys (.env.local)           │ │
│  │ Database Credentials               │ │
│  │ No Hardcoded Secrets               │ │
│  └────────────────────────────────────┘ │
│                                          │
└──────────────────────────────────────────┘
```

## Technology Stack

```
┌────────────────────────────────────────────┐
│           Frontend Stack                   │
├────────────────────────────────────────────┤
│ • Next.js 15 (React Framework)            │
│ • React 19 (UI Library)                   │
│ • TypeScript (Type Safety)                │
│ • Tailwind CSS (Styling)                  │
│ • NextAuth (Authentication)               │
│ • React Icons (UI Icons)                  │
│ • browser-image-compression (Compression) │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│           Backend Stack                    │
├────────────────────────────────────────────┤
│ • Next.js API Routes (REST API)           │
│ • Mongoose (MongoDB ODM)                  │
│ • bcryptjs (Password Hashing)             │
│ • JWT (Session Tokens)                    │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│           Database Stack                   │
├────────────────────────────────────────────┤
│ • MongoDB (NoSQL Database)                │
│ • Mongoose Schemas (Data Models)          │
│ • Indexes (Performance)                   │
└────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌──────────────────────────────────────────┐
│         Production Environment           │
├──────────────────────────────────────────┤
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  Vercel (Hosting)                  │ │
│  │  ├── Next.js Application           │ │
│  │  ├── API Routes                    │ │
│  │  ├── Static Assets                 │ │
│  │  └── Environment Variables         │ │
│  └────────────────────────────────────┘ │
│              │                           │
│              │ (HTTPS)                   │
│              ▼                           │
│  ┌────────────────────────────────────┐ │
│  │  MongoDB Atlas (Database)          │ │
│  │  ├── User Collection               │ │
│  │  ├── Image Collection              │ │
│  │  ├── Indexes                       │ │
│  │  └── Backups                       │ │
│  └────────────────────────────────────┘ │
│                                          │
└──────────────────────────────────────────┘
```

## File Structure Details

```
BetterEditor/
│
├── app/                      # Next.js App Router
│   ├── api/                  # Backend API Routes
│   │   ├── auth/
│   │   │   ├── [...nextauth]/route.ts  # Auth handler
│   │   │   └── register/route.ts       # Registration
│   │   └── images/
│   │       ├── route.ts                # List/Create
│   │       └── [id]/route.ts           # Get/Update/Delete
│   ├── auth/                 # Auth Pages
│   │   ├── signin/page.tsx
│   │   └── signup/page.tsx
│   ├── layout.tsx            # Root Layout
│   ├── page.tsx              # Home (Dashboard)
│   └── globals.css           # Global Styles
│
├── components/               # React Components
│   ├── Dashboard.tsx         # Main UI
│   ├── PhotoEditor.tsx       # Editor Modal
│   └── ImageUpload.tsx       # Upload Modal
│
├── lib/                      # Utilities
│   └── mongodb.ts            # DB Connection
│
├── models/                   # Database Models
│   ├── User.ts               # User Schema
│   └── Image.ts              # Image Schema
│
├── types/                    # TypeScript Types
│   ├── mongoose.d.ts
│   └── next-auth.d.ts
│
├── public/                   # Static Files
│
├── .env.local                # Environment Vars
├── package.json              # Dependencies
├── tsconfig.json             # TypeScript Config
├── next.config.ts            # Next.js Config
└── tailwind.config.ts        # Tailwind Config
```

---

This architecture provides:

- ✅ Scalability (serverless functions)
- ✅ Security (multiple layers)
- ✅ Performance (optimized queries)
- ✅ Maintainability (clear structure)
- ✅ Type Safety (TypeScript throughout)
- ✅ Developer Experience (organized code)
